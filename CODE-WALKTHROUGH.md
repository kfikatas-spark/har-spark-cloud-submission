# Παρουσίαση της υλοποίησης

Στην ενότητα αυτή παρουσιάζω τα βασικά αρχεία του έργου και τον ρόλο τους: από την ανάγνωση των μετρήσεων έως την εκπαίδευση και την αξιολόγηση των μοντέλων, τοπικά και στο Google Cloud.

## Αντικείμενο της εφαρμογής

Η εφαρμογή χρησιμοποιεί μετρήσεις από κινητά τηλέφωνα και έξυπνα ρολόγια του συνόλου δεδομένων WISDM. Κάθε μέτρηση συνοδεύεται από την ετικέτα της δραστηριότητας που εκτελούσε το άτομο τη στιγμή της καταγραφής. Από διαδοχικές μετρήσεις υπολογίζονται χαρακτηριστικά της κίνησης, με τα οποία εκπαιδεύονται δύο μοντέλα αναγνώρισης δραστηριότητας.

Τα δεδομένα χωρίζονται ανά άτομο: οι καταγραφές του ίδιου ατόμου ανήκουν αποκλειστικά στο σύνολο εκπαίδευσης, επικύρωσης ή δοκιμής. Έτσι, η τελική αξιολόγηση γίνεται με δεδομένα ατόμων που δεν χρησιμοποιήθηκαν στην εκπαίδευση.

## Στάδια επεξεργασίας

**Ανάγνωση μετρήσεων → Υπολογισμός χαρακτηριστικών ανά χρονικό παράθυρο → Συνδυασμός δεδομένων αισθητήρων → Διαχωρισμός δεδομένων ανά άτομο → Εκπαίδευση και αξιολόγηση → Αποθήκευση αποτελεσμάτων**

Το κύριο πακέτο Python βρίσκεται στον φάκελο [`supporting/src/har_spark/`](supporting/src/har_spark/).

| Αρχείο | Ρόλος |
|---|---|
| [`ingest.py`](supporting/src/har_spark/ingest.py) | Διαβάζει τις γραμμές των αρχείων WISDM, αναλύει τα πεδία τους και κρατά τις έγκυρες μετρήσεις της επιλεγμένης συσκευής και των αισθητήρων. |
| [`features.py`](supporting/src/har_spark/features.py) | Υπολογίζει χαρακτηριστικά ανά χρονικό παράθυρο, συνδυάζει τα χαρακτηριστικά των αισθητήρων και χωρίζει τα δεδομένα σε σύνολα εκπαίδευσης, επικύρωσης και δοκιμής με βάση το άτομο από το οποίο προέρχονται. |
| [`model.py`](supporting/src/har_spark/model.py) | Ορίζει τα στάδια προετοιμασίας και εκπαίδευσης για τη Λογιστική Παλινδρόμηση και το Random Forest. Υπολογίζει τους δείκτες αξιολόγησης και τα στοιχεία του πίνακα σύγχυσης. |
| [`pipeline.py`](supporting/src/har_spark/pipeline.py) | Συνδέει τα επιμέρους στάδια και αποθηκεύει τα αποτελέσματα κάθε πειράματος. |
| [`config.py`](supporting/src/har_spark/config.py) | Φορτώνει τις ρυθμίσεις του πειράματος, όπως τις διαδρομές των αρχείων, τους αισθητήρες και τη διάρκεια του χρονικού παραθύρου. |
| [`spark.py`](supporting/src/har_spark/spark.py) | Δημιουργεί τη συνεδρία Spark με τις επιλεγμένες ρυθμίσεις εκτέλεσης. |
| [`cli.py`](supporting/src/har_spark/cli.py) | Επιτρέπει την εκτέλεση από τη γραμμή εντολών, με τις επιλογές `prepare`, `train` και `run`. |

## Πειράματα και εκτέλεση

- Το [δείγμα πραγματικών δεδομένων](examples/wisdm-sample/) δείχνει τη μορφή των αρχείων εισόδου. Για την επανάληψη των πειραμάτων απαιτείται το πλήρες WISDM.
- Στα πειράματα χρησιμοποιήθηκαν σταθερές υπερπαράμετροι. Το σύνολο επικύρωσης διατηρήθηκε χωριστά, αλλά δεν χρησιμοποιήθηκε για αυτοματοποιημένη αναζήτηση υπερπαραμέτρων.
- Τα αποτελέσματα του αποθετηρίου προέρχονται από τις προηγούμενες εκτελέσεις των πειραμάτων.
- Το πρόγραμμα εκτέλεσης στο Google Cloud και οι σχετικές οδηγίες βρίσκονται στο [`supporting/cloud/`](supporting/cloud/). Χρησιμοποιεί κοινές συναρτήσεις με το τοπικό πακέτο, αλλά συνδέει τα στάδια μέσα σε χωριστό πρόγραμμα.
- Η εγκατάσταση, η λήψη των δεδομένων και οι εντολές εκτέλεσης περιγράφονται στον [οδηγό εκτέλεσης](supporting/README.md).
